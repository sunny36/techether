<div class="row">
	<div class="col-md-4 col-md-offset-4 headspace">
		<% @subjects.each do |subject| %>
			<div class="subjects">
				<h2><%= subject.name %></h2>
				<p><%= subject.description %></p>
			</div>
		<% end %>
	</div>
</div>